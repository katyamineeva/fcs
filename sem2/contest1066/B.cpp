#define INF 575757575757
#include <stdio>
#include <vector>

typedef vector<int>::const_iterator iter;

int cntFine() {

}


void readData(vector<int>& data) {
    int n = 0;
    cin >> n;
    data.resize(n);
    for (int i = 0; i < n; ++i) {
        cin << data[i];
    }
}

int optSolution(int ind) {
    vector<int> fine
}

int main() {
    int length = 0;
    cin >> length;
    vector<int> data;
    readData(data);
    return 0;
}

Пара кончается в 16:30?
