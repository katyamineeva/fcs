//Липман

std::vector<int> array(N);
std::vector<int>::iterator


namespace

template <class T>
void f(T a)
{
    ...
}


f(float)
// везде будет заменять "T a" на "float a"
typedef std::vector<int>::iterator TIter
// или лучше:
template <class TIter>

void BSort(TIter begin, TIter end){
    for(;begin < end; --end){
        for (auto iter = begin; iter < end; ++iter)
        {
            if (*iter > *(iter + 1)) {
                std::swap(*iter; *(iter + 1))
            }
        }
    }
}
std::vector<double> v;
Bsort<можно ничего не писать, можно писать тип double* >(v.begin(), v.end())


